# 6.	从键盘输入两个向量，每个向量的维度是2，
# 向量中每个元素的范围在0到1之间，计算两个向量的余弦相似度，并输出结果。
import math
x1 = 0.1
y1 = 0.5
x2 = 0.3
y2 = 0.9
m = (x1, y1)
n = (x2, y2)
sim = (x1*x2+y1*y2)/(math.sqrt(x1**2+y1**2)*(x2**2+y2**2))
print(sim)
